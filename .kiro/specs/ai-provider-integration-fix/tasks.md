# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - API Key Plaintext Storage and Base URL Loss
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bugs exist
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bugs exist
  - **Scoped PBT Approach**: Scope the property to concrete failing cases:
    - Save provider with API key "sk-test-123" and verify it's stored as plaintext (not encrypted)
    - Save OpenRouter with base URL "https://openrouter.ai/api/v1" and verify baseUrl is undefined after retrieval
    - Save Ollama with base URL "http://localhost:11434/v1" and verify baseUrl is undefined after retrieval
  - Test implementation details from Bug Condition in design:
    - For API key encryption: `isBugCondition(input) = input.apiKey IS NOT EMPTY AND NOT isEncrypted(input.apiKey)`
    - For base URL: `isBugCondition(input) = input.baseUrl IS PROVIDED AND NOT isPersisted(input.baseUrl)`
  - The test assertions should match the Expected Behavior Properties from design:
    - API keys SHALL be encrypted using `encryptString()` before storage
    - Base URLs SHALL be persisted to the database and retrievable
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bugs exist)
  - Document counterexamples found:
    - API key stored in plaintext format in IndexedDB
    - Base URL field is undefined or null after saving
  - Mark task complete when test is written, run, and failures are documented
  - _Requirements: 1.1, 1.2, 2.1, 2.2_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Non-Buggy Provider Operations
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs:
    - Retrieve all providers → observe data structure and fields returned
    - Set provider as default → observe default flag behavior
    - Delete a provider → observe deletion and default promotion behavior
    - Render provider cards → observe UI display (connection status, masked keys, edit buttons)
    - Change provider model → observe model update behavior
    - Save provider without changing API key → observe key preservation
    - Load provider with legacy plaintext key → observe decryption fallback
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements:
    - For all read operations: `getProviders_fixed() = getProviders_original()`
    - For all default selections: `setDefault_fixed(id) = setDefault_original(id)`
    - For all deletions: `deleteProvider_fixed(id) = deleteProvider_original(id)`
    - For all UI renders: `renderProviderCard_fixed(p) = renderProviderCard_original(p)`
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.8, 3.9_

- [x] 3. Fix for AI Provider Integration Bugs

  - [x] 3.1 Add encryption to useAIProviders hook
    - Import `encryptString` and `decryptString` from `@/lib/security`
    - Update `saveProvider` function to encrypt API key before storing: `encryptedKey = await encryptString(input.apiKey.trim())`
    - Update `AIProviderSummary` interface to include `baseUrl?: string` field
    - Update `providersQuery` to include baseUrl in the returned summary
    - Preserve existing baseUrl when saving without changing it: `baseUrl: input.baseUrl ?? existing?.baseUrl`
    - _Bug_Condition: `(input.apiKey IS NOT EMPTY AND NOT isEncrypted(input.apiKey)) OR (input.baseUrl IS PROVIDED AND NOT isPersisted(input.baseUrl))`_
    - _Expected_Behavior: `encryptString(apiKey)` called before storage, `baseUrl` persisted to database_
    - _Preservation: Read operations, default selection, deletion, UI display, model selection, toast notifications, legacy key decryption_
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.4, 2.5, 3.1, 3.2, 3.7, 3.8, 3.9_

  - [x] 3.2 Update SettingsPage to pass baseUrl parameter
    - Update `handleSaveProvider` signature to accept `baseUrl?: string` parameter
    - Pass `baseUrl` to `saveProvider` call: `await saveProvider({ provider, apiKey, model, isDefault, baseUrl })`
    - Ensure ProviderCard passes baseUrl when calling onSaveProvider
    - _Bug_Condition: `input.baseUrl IS PROVIDED AND NOT isPersisted(input.baseUrl)`_
    - _Expected_Behavior: `baseUrl` parameter propagated through save flow_
    - _Preservation: UI display, toast notifications, provider card rendering_
    - _Requirements: 2.1, 2.2, 2.4, 2.5, 3.4, 3.5, 3.6_

  - [x] 3.3 Update ProviderSettingsSection callback signature
    - Update `onSaveProvider` prop type to include `baseUrl?: string` parameter
    - Ensure ProviderCard component passes baseUrl when calling the callback
    - Verify base URL input field is displayed for OpenRouter and Ollama providers
    - _Bug_Condition: `input.baseUrl IS PROVIDED AND NOT isPersisted(input.baseUrl)`_
    - _Expected_Behavior: `baseUrl` parameter accepted in callback signature_
    - _Preservation: Provider card UI, edit functionality_
    - _Requirements: 2.4, 2.5, 3.4, 3.5_

  - [x] 3.4 Remove unused providers from catalog
    - Remove `deepseek`, `groq`, `qwen`, `lmstudio`, `custom` from `PROVIDER_CATALOG` in `src/lib/ai/providerCatalog.ts`
    - Update `PROVIDER_ORDER` array to only include: `["anthropic", "openai", "google", "openrouter", "ollama"]`
    - Verify `AIProvider` type only allows the 5 core providers
    - _Bug_Condition: `input.provider IN ['deepseek', 'groq', 'qwen', 'lmstudio', 'custom']`_
    - _Expected_Behavior: Only 5 core providers available in catalog_
    - _Preservation: Provider selection, UI display for existing 5 providers_
    - _Requirements: User's audit request, 3.4_

  - [x] 3.5 Update OnboardingProviderStep (if exists)
    - Check if `src/components/onboarding/OnboardingProviderStep.tsx` exists
    - If exists, update provider save call to pass `baseUrl` parameter
    - Verify encryption is used via the updated `saveProvider` function
    - If file doesn't exist, skip this task
    - _Bug_Condition: `input.baseUrl IS PROVIDED AND NOT isPersisted(input.baseUrl)`_
    - _Expected_Behavior: Onboarding flow uses encrypted save with baseUrl support_
    - _Preservation: Onboarding flow functionality_
    - _Requirements: 2.4, 2.5, 2.7, 2.8, 2.9_

  - [x] 3.6 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - API Key Encryption and Base URL Persistence
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bugs are fixed)
    - Verify:
      - API keys are encrypted (base64 blob, not plaintext) in IndexedDB
      - `decryptString()` correctly recovers plaintext keys
      - Base URLs are persisted and retrievable for OpenRouter and Ollama
      - Only 5 providers exist in PROVIDER_CATALOG
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6_

  - [x] 3.7 Verify preservation tests still pass
    - **Property 2: Preservation** - Non-Buggy Provider Operations
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix:
      - Read operations return same data structure
      - Default provider selection works identically
      - Provider deletion and default promotion work identically
      - UI display (provider cards, masked keys, edit buttons) works identically
      - Model selection works identically
      - Toast notifications work identically
      - Legacy plaintext key decryption fallback works
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.8, 3.9_

- [x] 4. Checkpoint - Ensure all tests pass
  - Run all property-based tests (bug condition + preservation)
  - Run unit tests for encryption/decryption functions
  - Run integration tests for Settings page and Onboarding flow
  - Verify OpenRouter and Ollama can make API calls with custom base URLs
  - Verify only 5 provider cards are displayed in Settings
  - Ask the user if questions arise or if manual testing is needed
